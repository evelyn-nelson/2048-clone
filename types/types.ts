type GameData = [
  [number, number, number, number],
  [number, number, number, number],
  [number, number, number, number],
  [number, number, number, number]
];

type CellData = {
  x: number;
  y: number;
  value: number;
};
